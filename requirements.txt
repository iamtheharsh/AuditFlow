# Core Dependencies
# =================

# PDF text extraction - lightweight, pure Python alternative to pdfminer
pypdf>=3.0.0

# HTTP client library for external API calls (httpbin.org, OpenAI, etc.)
requests>=2.31.0

# HTML/XML parsing for URL-based text extraction
beautifulsoup4>=4.12.0

# Modern CLI framework - cleaner than argparse, supports auto-completion
typer>=0.9.0

# Progress bars for long-running operations (text extraction, scanning)
tqdm>=4.66.0

# Development & Testing Dependencies
# ==================================

# Testing framework
pytest>=7.4.0

# Code formatting (optional but recommended)
black>=23.0.0

# Import sorting (optional but recommended)
isort>=5.12.0

# Type checking (optional but recommended)
mypy>=1.5.0

# Documentation generation (optional)
# sphinx>=7.0.0

# OpenAI Integration (Optional - uncomment if using LLM features)
# ===============================================================
# OpenAI API client for LLM-based document analysis
# openai>=1.3.0

# Additional PDF processing (alternative to pypdf - more features but heavier)
# ===============================================================
# Advanced PDF text extraction with layout analysis
# pdfminer.six>=20220524

# Web UI Framework (Optional - for human approval GUI in Phase 2)
# ===============================================================
# Minimal web framework for approval UI
# flask>=3.0.0
